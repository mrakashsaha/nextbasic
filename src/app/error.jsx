"use client"
import React from 'react';

const Error = ({error}) => {
    console.log(error)
    return (
        <div>
            <h2>There is a Big Error</h2>
        </div>
    );
};

export default Error;