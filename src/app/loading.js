import React from 'react';

const Loading = () => {
    return (
        <div>
            <h1>Loading...</h1>
            <p>Fetching the requested data</p>
        </div>
    );
};

export default Loading;